*** Begin Patch
*** Update File: .github/workflows/release.yml
@@
-      - name: Build Windows Installer (.exe)
-        run: bunx electron-builder --win --x64
+      - name: Build Windows Installer (.exe)
+        run: bunx electron-builder --win --x64 -c.publish=never
+        env:
+          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
*** End Patch