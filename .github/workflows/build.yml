*** Begin Patch
*** Update File: .github/workflows/build.yml
@@
-      - name: Build Android Release APK
-        run: |
-          cd android
-          chmod +x gradlew
-          ./gradlew assembleRelease || ./gradlew assembleDebug
+      - name: Ensure Gradle Wrapper is available
+        run: |
+          if [ ! -f "android/gradle/wrapper/gradle-wrapper.jar" ]; then
+            echo "Gradle wrapper missing — downloading distribution and extracting wrapper jar..."
+            mkdir -p android/gradle/wrapper
+            wget -q -O /tmp/gradle-dist.zip https://services.gradle.org/distributions/gradle-8.11.1-all.zip
+            unzip -q /tmp/gradle-dist.zip 'gradle-8.11.1/lib/gradle-wrapper.jar' || true
+            if [ -f gradle-8.11.1/lib/gradle-wrapper.jar ]; then
+              mv gradle-8.11.1/lib/gradle-wrapper.jar android/gradle/wrapper/gradle-wrapper.jar
+            fi
+            rm -rf gradle-8.11.1 /tmp/gradle-dist.zip || true
+          fi
+          chmod +x android/gradlew || true
+
+      - name: Build Android Release APK
+        run: |
+          cd android
+          chmod +x gradlew
+          ./gradlew assembleRelease || ./gradlew assembleDebug
*** End Patch